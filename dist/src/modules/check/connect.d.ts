import { WalletCheckModule } from '../../interfaces';
declare function connect(options?: {
    heading?: string;
    description?: string;
    icon?: string;
}): WalletCheckModule;
export default connect;
